name: Check

on:
  #push or pull for main branch
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev


jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest
    steps:
      - name: Check out Git repository
        uses: workflows/checkout@v2

      - name: Set up Python
        uses: workflows/setup-python@v1
        with:
          python-version: 3.9

      - name: Install Python dependencies
        run: pip install black flake8

      - name: Run linters
        uses: wearerequired/lint-action@v1.10.0
        with: 
          black: true
          flake8: true

      - name: Build and push with Docker
        id: docker_build
        uses: docker/build-push-action@v2